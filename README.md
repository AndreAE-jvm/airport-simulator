# Airport Simulator - Симулятор Аэропорта
# Проект "Симулятор Аэропорта" представляет собой объектно-ориентированное приложение на Python, моделирующее работу аэропорта по управлению рейсами, назначению выходов, обработке статусов рейсов и ведению лога событий.

## 📁 Структура проекта

Основные файлы:
plane.py - Модель самолета/рейса
Класс Plane - представляет отдельный рейс со всей необходимой информацией

Назначение: Управление состоянием отдельного рейса, проверка корректности операций

airport_simulator.py - Управление аэропортом
Класс AirportSimulator - центральный класс для управления всеми рейсами

Назначение: Координация работы всех рейсов, логирование событий, экспорт данных

main.py - Демонстрация работы
Назначение: Пример использования классов, тестирование функциональности

## 🛠 Технические детали

## Класс Plane

Поля (атрибуты):
flight_number (str) - номер рейса (уникальный идентификатор)

airline (str) - название авиакомпании

destination (str) - пункт назначения

status (str) - текущий статус рейса:

"scheduled" - по расписанию

"boarding" - идет посадка

"departed" - вылетел

"delayed" - задержан

gate (str/None) - номер выхода (гейта)

Основные методы:
assign_gate(gate) - назначить выход на посадку

start_boarding() - начать посадку пассажиров

delay(reason) - задержать рейс с указанием причины

depart() - отметить вылет рейса

to_dict() - сериализация объекта в словарь

from_dict(data) - статический метод для десериализации

Особенности:
Автоматическая проверка корректности переходов между статусами

Предотвращение некорректных операций (например, вылет без посадки)

## Класс AirportSimulator

Хранилища данных:
scheduled_flights (list) - список всех рейсов

log (list) - журнал всех событий с временными метками

Основные методы:
add_flight(plane) - добавить рейс в расписание

assign_gate(flight_number, gate) - назначить выход для конкретного рейса

start_boarding(flight_number) - начать посадку на рейс

delay_flight(flight_number, reason) - задержать рейс

depart_flight(flight_number) - отправить рейс

display_status() - отобразить таблицу статусов всех рейсов

export_log(filename) - экспортировать лог в JSON файл

Вспомогательные методы (protected):
_find_flight(flight_number) - поиск рейса по номеру


📊 Логирование событий
Система ведет подробный лог всех операций в формате JSON:

json
[
  {
    "timestamp": "2025-11-22T22:01:34.971655",
    "event": "Добавлен рейс SU100",
    "details": "Аэрофлот в Лондон"
  }
]
## Формат временных меток: ISO 8601 - международный стандарт для однозначной интерпретации дат и времени ("timestamp": datetime.now().isoformat().

🚀 Запуск и использование
Требования:
Python 3.6+
Стандартные библиотеки: json, datetime

Запуск:
bash
python main.py
Пример использования в коде:
python
from plane import Plane
from airport_simulator import AirportSimulator

## Создание аэропорта
airport = AirportSimulator()

## Добавление рейсов
flight = Plane("SU100", "Аэрофлот", "Лондон")
airport.add_flight(flight)

## Работа с рейсами
airport.assign_gate("SU100", "A15")
airport.start_boarding("SU100")
airport.depart_flight("SU100")

## Просмотр статуса
airport.display_status()

## Экспорт лога
airport.export_log("airport_log.json")

### 🔄 Жизненный цикл рейса
Типичная последовательность операций с рейсом:

Создание → Plane("SU100", "Аэрофлот", "Лондон")

Добавление в расписание → add_flight(plane)

Назначение выхода → assign_gate("SU100", "A15")

Начало посадки → start_boarding("SU100")

Вылет → depart_flight("SU100")

Примечание: На любом этапе рейс может быть задержан с указанием причины.

### 💡 Особенности реализации
Обработка ошибок:
Проверка существования рейсов перед операциями

Валидация допустимых переходов между статусами

Предотвращение некорректных операций (например, вылет без посадки)

Архитектурные решения:
Разделение ответственности между классами

Использование protected методов для внутренней логики

Поддержка сериализации/десериализации для сохранения состояния

Гибкая система логирования с временными метками

Возможности расширения:
Легко добавить новые статусы рейсов

Можно реализовать дополнительные проверки

Возможно добавление новых типов событий в лог

📈 Пример вывода display_status()
text
=== ТЕКУЩИЙ СТАТУС РЕЙСОВ ===
Рейс      Авиакомпания   Направление    Статус       Выход 
-----------------------------------------------------------------
SU100     Аэрофлот       Лондон         посадка      A15   
BA123     British Air... Нью-Йорк       задержан     B22   
LH456     Lufthansa      Берлин         по распис... ---
Этот проект демонстрирует принципы ООП, работу с файлами JSON, управление состоянием объектов и создание сложных систем с взаимодействующими компонентами.
